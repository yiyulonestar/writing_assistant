# 使用说明 —— 网文创作辅助 Agent 后端

本文档面向**接口使用者**，介绍如何启动服务、用 FastAPI 自带的 Swagger 文档（`/docs`）测试接口，以及如何按顺序完成一部小说的「设定 → 生成 → 审稿 → 重写」完整流程。

> 开发相关说明见同目录《开发文档.md》。前端暂未开发，现阶段所有接口均可通过 Swagger UI 直接调用。

---

## 目录

1. [环境准备与启动](#一环境准备与启动)
2. [用 Swagger 文档测试](#二用-swagger-文档测试)
3. [鉴权说明](#三鉴权说明)
4. [数据模型字段速查](#四数据模型字段速查)
5. [接口详解](#五接口详解)
6. [完整使用流程示例](#六完整使用流程示例)
7. [常见问题](#七常见问题)

---

## 一、环境准备与启动

### 1. 前置依赖

- Python 3.14
- Docker（提供 PostgreSQL 16 + pgvector）
- [uv](https://docs.astral.sh/uv/)（包管理工具）

### 2. 启动步骤

```bash
# 1. 起数据库（PostgreSQL + pgvector）
docker compose up -d db

# 2. 安装依赖（uv 按 uv.lock 精确安装；含 dev 工具与本地 embedding）
uv sync --extra dev --extra embeddings

# 3. 配置环境变量（backend/.env）
#    至少需要 DASHSCOPE_API_KEY=sk-xxx

# 4. 启动服务
uv run uvicorn app.main:app --reload
```

启动成功后：

- **Swagger 文档**：http://localhost:8000/docs （推荐，可视化测试）
- **ReDoc 文档**：http://localhost:8000/redoc
- **OpenAPI JSON**：http://localhost:8000/openapi.json

> 开发环境启动时会在 lifespan 中自动建表、确保 pgvector 扩展、预加载嵌入模型，无需手动迁移。

### 3. 关键环境变量（`backend/.env`）

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `DASHSCOPE_API_KEY` | 通义千问 / DashScope API Key | 空 |
| `GENERATION_MODEL` | 正文生成 / 拆解用模型 | `qwen3.5-flash` |
| `REVIEW_MODEL` | 审稿用模型 | `qwen3.6-35b-a3b` |
| `EMBEDDING_PROVIDER` | `huggingface`（本地）或 `openai`（远程） | `huggingface` |
| `EMBEDDING_MODEL` | 嵌入模型名 | `BAAI/bge-small-zh-v1.5` |
| `EMBEDDING_DIMENSION` | 嵌入向量维度 | `512` |
| `API_KEY` | 可选鉴权 Key（设置后生成类接口需携带 `X-API-Key`） | 空 |

---

## 二、用 Swagger 文档测试

打开 http://localhost:8000/docs 后，页面会列出所有接口，按 tag 分组（novels / characters / chapters / timeline / world-settings / generate / health）。

基本用法：

1. 展开某个接口（如 `POST /api/v1/novels`）。
2. 点 **Try it out**。
3. 在请求体里填入 JSON。
4. 点 **Execute** 发送，下方即显示响应状态码与返回体。

> 生成的 `id` 都是 UUID，后续操作（修改/删除/关联）需要把它复制到对应位置，注意别手滑改错。

---

## 三、鉴权说明

当前是**轻量 API Key 鉴权**，默认关闭：

- 若 `.env` 里**未设置** `API_KEY`：所有接口无需鉴权，直接调用。
- 若设置了 `API_KEY`：**生成类接口**（`/generate/*`）需要请求头 `X-API-Key: <你的Key>`，否则返回 `401`。

CRUD 类接口（novels/characters/chapters/timeline/world-settings）目前不受鉴权影响。

在 Swagger 里设置鉴权：点右上角 **Authorize**，填入 `X-API-Key` 的值即可。

---

## 四、数据模型字段速查

### 4.1 Novel（小说）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `title` | string | ✅ | 书名 |
| `genre` | string | | 类型（仙侠 / 都市 / 科幻…） |
| `synopsis` | string | | 全书简介（供拆解/摘要用，建议认真写） |
| `notes` | string | | 备注 |

### 4.2 Character（角色）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `novel_id` | UUID | ✅ | 所属小说 |
| `name` | string | ✅ | 角色名 |
| `aliases` | string[] | | 别名 / 称号 |
| `role` | string | | 定位（主角 / 配角 / 反派…） |
| `personality` | string | | 性格（影响审稿的一致性校验） |
| `background` | string | | 背景故事 |
| `appearance` | string | | 外貌 |
| `goals` | string | | 动机 / 目标 |
| `relationships` | object | | 与其他角色的关系（自由 JSON） |
| `notes` | string | | 备注 |

### 4.3 WorldSetting（世界观设定）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `novel_id` | UUID | ✅ | 所属小说 |
| `category` | string | ✅ | 分类（宗门 / 功法 / 地理 / 势力 / 物品…） |
| `name` | string | ✅ | 名称 |
| `description` | string | | 描述 |
| `parent_id` | UUID | | 父级设定（层级，如：某大陆 → 某国 → 某城） |
| `notes` | string | | 备注 |

### 4.4 TimelineEvent（时间线事件）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `novel_id` | UUID | ✅ | 所属小说 |
| `title` | string | ✅ | 事件标题 |
| `description` | string | | 事件描述 |
| `time_point` | string | | 时间点（网文多为相对/虚数时间，用文本） |
| `order_index` | int | | 排序（越小越靠前） |
| `status` | string | | `planned`（计划）/ `occurred`（已发生） |
| `chapter_id` | UUID | | 关联章节 |
| `involved_character_ids` | string[] | | 涉及角色 ID 列表 |

### 4.5 Chapter（章节）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `novel_id` | UUID | ✅ | 所属小说 |
| `number` | int | ✅ | 章节序号 |
| `title` | string | | 标题 |
| `summary` | string | | 本章摘要（生成时自动填写） |
| `outline` | string | | 本章大纲 |
| `content` | string | | 正文 |
| `word_count` | int | | 字数（按中文字符计，写入正文时自动计算） |
| `status` | string | | `draft` / `revising` / `done` |

### 4.6 ChapterDraft（草稿版本）

| 字段 | 类型 | 说明 |
|------|------|------|
| `chapter_id` | UUID | 所属章节 |
| `version` | int | 版本号（自动递增） |
| `content` | string | 该版本正文 |
| `note` | string | 本次修改说明 |

---

## 五、接口详解

> 所有路径都带 `/api/v1` 前缀。以下按模块列出。

### 5.1 健康检查

**`GET /api/v1/health`**

返回 `{"status": "ok"}`，用于确认服务存活。

### 5.2 小说 Novels

| 方法 | 路径 | 说明 |
|------|------|------|
| `POST` | `/novels` | 创建小说 |
| `GET` | `/novels` | 列出所有小说 |
| `GET` | `/novels/{novel_id}` | 获取单本 |
| `PATCH` | `/novels/{novel_id}` | 更新 |
| `DELETE` | `/novels/{novel_id}` | 删除（级联删除其角色/章节等） |

**创建示例**

```json
{
  "title": "凡人修仙",
  "genre": "仙侠",
  "synopsis": "山村少年林辰偶然得到神秘玉简，踏上修仙之路，从底层一步步崛起。",
  "notes": "第一部"
}
```

### 5.3 角色 Characters

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/characters?novel_id=xxx` | 列出某小说的角色（**novel_id 必填**） |
| `POST` | `/characters` | 创建角色 |
| `GET` | `/characters/{character_id}` | 获取单个 |
| `PATCH` | `/characters/{character_id}` | 更新 |
| `DELETE` | `/characters/{character_id}` | 删除 |

**创建示例**

```json
{
  "novel_id": "你的小说id",
  "name": "林辰",
  "aliases": ["小辰", "林师兄"],
  "role": "主角",
  "personality": "坚韧、冷静、重情义",
  "background": "山村少年，被家族轻视",
  "appearance": "剑眉星目，身形修长",
  "goals": "追求长生，保护亲友",
  "relationships": {"苏瑶": "道侣", "张长老": "恩师"}
}
```

> 创建/更新角色时会自动同步 embedding（语义检索用）。若未安装本地 embedding 且未配置远程 provider，会跳过并在日志告警，不影响 CRUD。

### 5.4 世界观设定 World Settings

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/world-settings?novel_id=xxx&category=宗门` | 列出（`category` 可选过滤） |
| `POST` | `/world-settings` | 创建 |
| `GET` | `/world-settings/{setting_id}` | 获取单个 |
| `PATCH` | `/world-settings/{setting_id}` | 更新 |
| `DELETE` | `/world-settings/{setting_id}` | 删除 |

**创建示例（含层级）**

```json
{
  "novel_id": "你的小说id",
  "category": "宗门",
  "name": "青云宗",
  "description": "正道大宗，招收弟子极其严格"
}
```

```json
{
  "novel_id": "你的小说id",
  "category": "地理",
  "name": "青云山",
  "parent_id": "上面的青云宗id",
  "description": "青云宗山门所在"
}
```

### 5.5 时间线 Timeline

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/timeline?novel_id=xxx` | 列出某小说时间线（按 `order_index` 排序） |
| `POST` | `/timeline` | 创建事件 |
| `GET` | `/timeline/{event_id}` | 获取单个 |
| `PATCH` | `/timeline/{event_id}` | 更新 |
| `DELETE` | `/timeline/{event_id}` | 删除 |

**创建示例**

```json
{
  "novel_id": "你的小说id",
  "title": "林辰觉醒废柴体质",
  "description": "被家族嘲笑，却偶得神秘玉简",
  "time_point": "第一章",
  "order_index": 1,
  "status": "occurred",
  "involved_character_ids": ["林辰的角色id"]
}
```

### 5.6 章节 Chapters

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/chapters?novel_id=xxx` | 列出某小说章节（按序号排序） |
| `POST` | `/chapters` | 创建章节（手工） |
| `GET` | `/chapters/{chapter_id}` | 获取单章 |
| `PATCH` | `/chapters/{chapter_id}` | 更新 |
| `DELETE` | `/chapters/{chapter_id}` | 删除 |

**手工创建章节示例**（通常用「生成」接口代替手工创建）

```json
{
  "novel_id": "你的小说id",
  "number": 1,
  "title": "第一章 废柴觉醒",
  "content": "第一章正文……",
  "status": "draft"
}
```

> `word_count` 会在写入 `content` 时自动按中文字符计算，无需手动填。

#### 草稿版本历史

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/chapters/{chapter_id}/drafts` | 列出该章所有草稿版本 |
| `POST` | `/chapters/{chapter_id}/drafts` | 手动保存一版草稿（`version` 自动递增） |
| `GET` | `/chapters/{chapter_id}/drafts/{version}` | 获取指定版本 |
| `GET` | `/chapters/{chapter_id}/drafts/{version}/diff?base=1` | 对比两个版本（unified diff） |

**保存草稿示例**

```json
{ "content": "改过的正文……", "note": "润色了一下对白" }
```

#### 局部重写

**`POST /chapters/{chapter_id}/rewrite`**

按段落范围重写（段落以**空行**分隔，下标从 0 起）。

```json
{
  "start": 1,
  "end": 2,
  "instruction": "把这段打斗写得更有画面感和张力"
}
```

- 重写后正文会更新到章节，并自动生成一个新的草稿版本（保留历史）。
- `instruction` 可省略，省略时模型会做通用润色。

### 5.7 生成 Generate

> 生成类接口耗时较长（尤其审稿模型较慢），单章 3000 字可能要几分钟。请耐心等待，或先用较小的 `target_word_count` 试跑。

#### 单章生成（确定性流水线）

**`POST /generate/chapter`**

请求体：

```json
{
  "novel_id": "你的小说id",
  "chapter_number": 1,
  "outline": "林辰在青云宗入门考核中崭露头角，得到长老赏识。",
  "target_word_count": 3000
}
```

- `outline` 可省略，省略时由 planner 自动拆解。
- 响应包含生成的正文、字数、审稿报告（`review`）。

响应示例：

```json
{
  "chapter_id": "uuid",
  "content": "正文……",
  "word_count": 2890,
  "review": {
    "issues": ["……"],
    "conflicts": ["……"],
    "fixed": true,
    "summary": "……"
  }
}
```

- `review.conflicts` 为空 → 审稿通过；非空且有 `fixed=false` → 表示已达到回退上限仍有冲突。

#### 批量生成

**`POST /generate/batch`**

```json
{
  "novel_id": "你的小说id",
  "start_chapter": 1,
  "count": 3,
  "target_word_count": 3000,
  "outlines": ["第一章大纲", "第二章大纲", null]
}
```

- 串行生成，每章自动复用前一章的前文摘要。
- `outlines` 按顺序对应各章，可为 `null`（该章自动拆解）。

#### 多 Agent 生成（LangGraph）

**`POST /generate/agent`**

请求体与单章生成相同，另可带查询参数 `thread_id`（用于断点续跑/隔离会话）：

```json
{
  "novel_id": "你的小说id",
  "chapter_number": 1,
  "outline": "……",
  "target_word_count": 3000
}
```

- 与 `/generate/chapter` 结果等价，但走 LangGraph 编排（可断点续跑、条件回退）。

#### WebSocket 进度推送

**`WS /generate/ws`**

连上后发送生成请求 JSON，服务端边生成边推送进度事件：

```json
{"stage": "planning", "chapter": 1}
{"stage": "retrieving", "chapter": 1}
{"stage": "generating", "chapter": 1}
{"stage": "reviewing", "chapter": 1, "round": 1}
{"stage": "persisting", "chapter": 1}
{"stage": "summarizing", "chapter": 1}
{"stage": "done", "chapter": 1, "chapter_id": "uuid"}
{"stage": "complete", "chapter_id": "uuid", "word_count": 2890}
```

---

## 六、完整使用流程示例

下面演示从零开始写一章的完整顺序。**注意：后一步会用到前一步返回的 `id`**，记得复制。

### 第 1 步：建小说

`POST /novels`

```json
{"title": "凡人修仙", "genre": "仙侠", "synopsis": "山村少年林辰偶然得到神秘玉简，踏上修仙之路。"}
```

→ 记下返回的 `id`（记为 `NOVEL_ID`）。

### 第 2 步：录入角色设定

`POST /characters`（可多次，录入主角、配角等）

```json
{"novel_id": "NOVEL_ID", "name": "林辰", "role": "主角", "personality": "坚韧、冷静、重情义", "background": "山村少年，被家族轻视"}
```

### 第 3 步：录入世界观

`POST /world-settings`

```json
{"novel_id": "NOVEL_ID", "category": "宗门", "name": "青云宗", "description": "正道大宗"}
```

### 第 4 步：录入时间线（可选）

`POST /timeline`

```json
{"novel_id": "NOVEL_ID", "title": "林辰觉醒废柴体质", "time_point": "第一章", "order_index": 1}
```

### 第 5 步：生成第一章

`POST /generate/chapter`

```json
{"novel_id": "NOVEL_ID", "chapter_number": 1, "outline": "林辰在入门考核中崭露头角。", "target_word_count": 2000}
```

→ 拿到正文与审稿报告。

### 第 6 步：局部重写不满意段落（可选）

`POST /chapters/{chapter_id}/rewrite`

```json
{"start": 0, "end": 2, "instruction": "开头再抓人一点，制造悬念"}
```

### 第 7 步：查看版本 / 对比（可选）

- `GET /chapters/{chapter_id}/drafts` 查看所有版本
- `GET /chapters/{chapter_id}/drafts/2/diff?base=1` 对比 v1 与 v2

### 第 8 步：继续批量生成后续章节

`POST /generate/batch`

```json
{"novel_id": "NOVEL_ID", "start_chapter": 2, "count": 5}
```

---

## 七、常见问题

**Q1：生成接口返回 500？**
看后端日志。常见原因：`DASHSCOPE_API_KEY` 未配置或无效、模型名不存在、网络不通。

**Q2：生成很慢？**
审稿模型 `qwen3.6-35b-a3b` 较大，单次审稿要几十秒。可在 `.env` 把 `REVIEW_MODEL` 换成更快的小模型。

**Q3：语义检索不生效 / 日志报「向量召回失败」？**
需要 PostgreSQL + pgvector 在跑（`docker compose up -d db`）。未起库时自动降级为结构化精确查询（按角色名/关键词），不影响生成。

**Q4：创建角色报错或很慢？**
默认 `EMBEDDING_PROVIDER=huggingface` 需要本地装了 `sentence-transformers`（`uv sync --extra embeddings`）。首次会下载模型权重。若不想本地跑，改用 `EMBEDDING_PROVIDER=openai`（注意同步改 `EMBEDDING_DIMENSION`）。

**Q5：怎么鉴权？**
在 `.env` 设置 `API_KEY`，之后生成类接口在 Swagger 里点 **Authorize** 填入 `X-API-Key`。

**Q6：`target_word_count` 和最终字数不一致？**
`word_count` 按中文字符统计，且是模型生成的实际长度，与目标字数可能有出入，属正常。
