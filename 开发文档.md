# 开发文档 —— 长篇小说 / 网文创作辅助 Agent 后端

> 本文档基于对 `backend/` 现有代码的完整梳理编写，描述**目标技术栈**、**架构设计**与**后续开发流程**。
> 后端代码已按**实际落地栈**改造完成：
> **FastAPI + Tortoise ORM + PostgreSQL(pgvector) + OpenAI SDK(通义千问) + BAAI/bge-small-zh（+ 可选 LangChain / LangGraph）**。
> 历史占位实现（Anthropic + SQLAlchemy + bge-m3）与迁移动作记录见下文「阶段 0」。

---

## 一、项目概览

网文 / 长篇小说创作辅助后端，核心能力：

| 能力 | 说明 |
|------|------|
| 结构化设定管理 | 角色 / 世界观 / 时间线存结构化表，embedding 列做语义检索 |
| 多步生成流水线 | 拆解 → 检索设定 → 生成草稿 → 审稿 → 落库 |
| 一致性审稿 | 独立审稿模块核对角色语气、剧情逻辑、设定违背 |
| 工具调用 | 百科检索 + 文本统计（本地确定性计算） |
| 交互式重写 | 按章节/段落局部重写，保留草稿版本历史 |
| （可选）多 Agent 编排 | 用 LangGraph 将流水线升级为可自治循环的 Agent 图 |

---

## 二、后端结构分析（现状）

### 2.1 目录结构

```
backend/
├── app/
│   ├── main.py                     # FastAPI 入口 + Tortoise 生命周期
│   ├── core/
│   │   └── config.py               # pydantic-settings 配置（通义千问 + bge-small-zh）
│   ├── db/
│   │   ├── tortoise.py             # Tortoise 初始化 + TORTOISE_ORM 配置
│   │   ├── fields.py               # 自定义 VectorField（映射 pgvector）
│   │   └── ensure.py               # 确保 pgvector 扩展存在
│   ├── models/                     # Tortoise 模型：novel / character / world / timeline / chapter
│   ├── schemas/                    # Pydantic：novel / character / chapter / timeline / world / generation
│   ├── api/
│   │   ├── deps.py                 # 鉴权占位
│   │   └── v1/
│   │       ├── router.py           # v1 路由聚合
│   │       └── endpoints/          # health / novels / characters / chapters / timeline / world-settings / generate
│   ├── services/
│   │   ├── stats.py                # ✅ 文本统计
│   │   ├── settings.py             # ✅ 设定检索 + embedding 同步 + 事实清单
│   │   └── generation/             # ✅ pipeline / planner / retriever / generator / reviewer
│   ├── llm/
│   │   ├── client.py               # ✅ 通义千问客户端（OpenAI SDK）
│   │   ├── tools.py                # ✅ 工具定义（text_stats / web_search）
│   │   └── prompts/                # ✅ planning / generation / review 提示词
│   ├── embeddings/
│   │   └── client.py               # ✅ bge-small-zh（huggingface / openai 双 provider）
│   └── utils/
├── tests/
│   └── test_health.py              # ✅ 健康检查异步测试
├── Dockerfile                      # 应用容器
├── docker-compose.yml              # ✅ db + app
├── entrypoint.sh                   # 容器启动：ensure pgvector + uvicorn
├── pyproject.toml                  # ✅ tortoise-orm + openai
└── README.md
```

### 2.2 数据模型（`app/models/`）

| 模型 | 表名 | 关键字段 | 关系 | embedding |
|------|------|----------|------|-----------|
| Novel | novels | title, genre, synopsis, notes | → chapters, characters | 无 |
| Character | characters | name, aliases(JSONB), role, personality, background, appearance, goals, relationships(JSONB) | → novel | ✅ |
| Chapter | chapters | number, title, summary, outline, content, word_count, status | → novel, → drafts | 无 |
| ChapterDraft | chapter_drafts | version, content, note | → chapter | 无 |
| TimelineEvent | timeline_events | title, description, time_point, order_index, status, involved_character_ids(JSONB) | → novel, → chapter | ✅ |
| WorldSetting | world_settings | category, name, description, parent_id(层级), notes | → novel | ✅ |

- 主键 UUID，`created_at` / `updated_at` 由 `DatetimeField(auto_now_add/auto_now)` 维护。
- embedding 列用自定义 `VectorField`（映射 pgvector `vector(dim)`），维度取自 `settings.embedding_dimension`。

### 2.3 当前实现状态

| 模块 | 状态 |
|------|------|
| FastAPI 入口 + 路由聚合 | ✅ 可运行 |
| 健康检查 `GET /api/v1/health` | ✅ |
| 小说 CRUD `novels.py`（完整竖切示例） | ✅ |
| ORM 模型（6 个）+ 关系 | ✅ |
| Pydantic Schema | ✅（novel / character / chapter / generation） |
| Tortoise 连接与会话管理 | ✅ |
| 文本统计 `stats.py` | ✅ |
| Docker Compose（db + app） | ✅ |
| 角色 / 章节 / 时间线 / 世界观 CRUD | ✅（含 embedding 同步） |
| LLM 客户端 / 嵌入编码 / 设定检索 | ✅ |
| 生成流水线 | ✅（planner → retriever → generator → reviewer，含回退修正） |
| Aerich 迁移文件 | 已移除，开发/生产均用 `generate_schemas` 自动建表 |
| 鉴权 | ✅ 简单 API Key（`X-API-Key` 请求头，未配置则不启用） |

### 2.4 与目标技术栈的差异（需迁移点）

| 位置 | 现状（占位） | 目标 |
|------|------------|------|
| `core/config.py` | `anthropic_api_key` / `claude-opus-5` / `claude-sonnet-5` / `bge-m3` / `1024` | `dashscope_api_key` / `qwen-max` / `qwen-plus` / `bge-small-zh-v1.5` / `512` |
| `llm/client.py` | `AsyncAnthropic` | `openai.AsyncOpenAI`（`base_url` 指向 DashScope 兼容端点） |
| `embeddings/client.py` | `BAAI/bge-m3` | `BAAI/bge-small-zh-v1.5` |
| `pyproject.toml` | `anthropic>=1.0` | `openai>=3.x`（+ 可选 `langchain` / `langgraph`） |
| ORM | `SQLAlchemy 2.0 + Alembic` | `Tortoise ORM 1.x`（`generate_schemas` 自动建表） |
| `.env` | `ANTHROPIC_API_KEY` 等 | `DASHSCOPE_API_KEY` 等 |

---

## 三、目标技术栈

### 3.1 基础栈

| 层 | 选型 | 说明 |
|----|------|------|
| 语言 | Python 3.14 | pyproject 已声明 `>=3.14` |
| Web 框架 | FastAPI | 全异步，自动 OpenAPI 文档 |
| ASGI 服务器 | Uvicorn | `--reload` 本地开发 |
| 包管理 | uv | 依赖锁定 `uv.lock`，`uv sync` 安装、`uv run` 运行 |
| ORM | Tortoise ORM 1.x | 原生 asyncio 异步 ORM，无需手动管理会话 |
| 驱动 | asyncpg | 异步 PostgreSQL 驱动（Tortoise 底层） |
| 数据库 | PostgreSQL 16 | Docker Compose 提供 |
| 向量扩展 | pgvector | 存 embedding 列 + 余弦相似度召回 |
| 迁移 | 开发环境自动建表（`generate_schemas`） |
| 校验 | Pydantic v2 + pydantic-settings | 已就绪 |
| 测试 | pytest + pytest-asyncio | `asyncio_mode=auto` |

### 3.2 LLM —— 通义千问（通过 OpenAI SDK 接入）

阿里云 DashScope 提供 **OpenAI 兼容模式**，直接用 `openai` SDK（当前 3.x）调用通义千问系列模型：

- **base_url**：`https://dashscope.aliyuncs.com/compatible-mode/v1`
- **认证**：`api_key` 填 `DASHSCOPE_API_KEY`（`sk-...`）
- **能力**：`chat.completions` 支持流式（`stream=True`）、函数调用（`tools`）、JSON 输出等。

推荐模型分工（可在 `.env` 覆盖）：

| 用途 | 推荐模型 | 说明 |
|------|----------|------|
| 正文生成 | `qwen-max` | 综合能力最强，长文创作质量最高 |
| 审稿 / 一致性检查 | `qwen-plus` | 性价比高，足够做结构化审稿 |
| 长上下文场景 | `qwen-long` | 超长上下文（~千万级 token），适合注入大量前文/设定 |
| 高速辅助 | `qwen-turbo` | 摘要、拆解等轻量任务 |

> 模型 ID 随版本演进（如 `qwen3-max` 等），以 DashScope 控制台最新模型名为准；统一收敛到 `.env` 配置，避免散落在代码里。

### 3.3 Embedding —— BAAI/bge-small-zh-v1.5

| 项 | 值 |
|----|----|
| 模型 | `BAAI/bge-small-zh-v1.5` |
| 维度 | **512** |
| 规模 | ~24M 参数，轻量，本地 CPU 即可推理 |
| 加载 | `sentence-transformers`，懒加载 |
| 适用 | 中文语义检索（角色/世界观/时间线的相似召回） |

**使用要点**：
- 检索时对 query 建议加前缀（bge 系列推荐指令）：
  `"为这个句子生成表示以用于检索相关文章：" + query`
- 编码后做 L2 归一化（`normalize_embeddings=True`），配合 pgvector 的余弦距离 `<->` 或 `<=>`。

### 3.4 多 Agent 编排 —— LangChain / LangGraph（可选）

当前流水线是**确定性编排**（planner → retriever → generator → reviewer），模型只在需要语言能力的环节介入，简单可控、易调试。

当后续需要「多 Agent 协作 / 自治循环 / 复杂控制流 / 可恢复执行」时，引入：

| 库 | 作用 |
|----|------|
| `langchain` | 提供集成：`ChatOpenAI`（指向 DashScope）、`PGVector` 向量库、`Embeddings` 封装、`Runnable` 链 |
| `langgraph` | 定义**有状态的 Agent 图**：节点=Agent，边=路由，支持条件分支、循环、`checkpointer` 断点续跑 |

典型 Agent 图（演进方向）：

```
           ┌──────────┐
  START ─▶ │ Planner  │ 拆解章节目标
           └────┬─────┘
                ▼
           ┌──────────┐
           │ Retriever│ 检索设定 → 事实清单
           └────┬─────┘
                ▼
           ┌──────────┐    有冲突/需改写
           │ Writer   │◀──────────────┐
           └────┬─────┘               │
                ▼                     │
           ┌──────────┐     ┌─────────┴───┐
           │ Reviewer │────▶│  Editor/修正 │
           └────┬─────┘     └─────────────┘
                │ 通过
                ▼
           落库 END
```

- 每个节点用 `qwen-max` / `qwen-plus` 等不同模型与提示词。
- 用 `StateGraph` 管理共享状态（`novel_id`、`chapter_number`、`outline`、`facts`、`draft`、`conflicts`、`round`）。
- 用 `checkpointer`（如 SQLite/Postgres saver）支持中途暂停、恢复、回溯。

---

## 四、核心架构设计

### 4.1 分层架构

```
api/v1/endpoints  （路由：参数校验 → 调 service）
        │
services          （业务编排：generation 流水线 / settings 检索）
        │
llm · embeddings  （外部能力封装：通义千问 / bge 编码）
        │
models · schemas  （ORM / Pydantic）
        │
db                （Tortoise 连接 + 自定义 VectorField）
```

依赖方向单向向下：`endpoints → services → llm/embeddings/db → models`。

### 4.2 生成流水线（RAG + 确定性编排）

```
用户请求（novel_id + chapter_number + outline?）
        │
        ▼
   ┌─────────┐  qwen-plus/turbo
   │ Planner │  拆解章节目标 → 大纲 + 涉及角色/地点/事件 + 情绪基调
   └────┬────┘
        ▼
   ┌──────────┐  结构化查询 + pgvector 向量召回
   │ Retriever│  组装「事实清单」（角色/世界观/时间线 渲染为紧凑文本）
   └────┬─────┘
        ▼
   ┌──────────┐  qwen-max（流式）
   │ Generator│  携带事实清单 + 前文上下文 → 生成草稿
   └────┬─────┘
        ▼
   ┌─────────┐  qwen-plus
   │ Reviewer│  审稿 → 结构化冲突列表（语气/逻辑/设定违背）
   └────┬────┘
        │ 有冲突 → 回退 Generator 修正（max_review_rounds=3）
        │ 无冲突
        ▼
     落库（Chapter + ChapterDraft）
```

### 4.3 设定检索（RAG 关键路径）

1. **结构化精确查询**：按 `novel_id` / `category` / 角色名精确查。
2. **向量召回**：对 `embedding` 列做余弦相似度召回（`embedding <=> :query` 升序取 top-k）。
3. **事实清单**：把命中的设定渲染为紧凑文本块（角色卡 / 世界观条目 / 时间线事件），注入生成与审稿提示词，约束一致性。

---

## 五、后续开发流程

> 估算按单人开发、每天投入计；可并行。P0 = 阻塞主线，P1 = 重要，P2 = 增强。

### 阶段 0：技术栈切换（0.5～1 天）

> 目标：把占位的 Anthropic / SQLAlchemy / bge-m3 替换为通义千问 / Tortoise / bge-small-zh。（✅ 已完成）

| # | 任务 | 涉及文件 | 优先级 |
|---|------|----------|--------|
| 0.1 | 配置改造：`dashscope_api_key`、`llm_base_url`、`generation_model=qwen-max`、`review_model=qwen-plus`、`embedding_provider=huggingface`、`embedding_model=BAAI/bge-small-zh-v1.5`、`embedding_dimension=512` | `core/config.py` | P0 |
| 0.2 | LLM 客户端换 `openai.AsyncOpenAI`（base_url 指向 DashScope） | `llm/client.py` | P0 |
| 0.3 | 嵌入客户端改为 `BAAI/bge-small-zh-v1.5`（512 维 + 归一化） | `embeddings/client.py` | P0 |
| 0.4 | 依赖替换：`anthropic` → `openai`；可选新增 `langchain` / `langgraph` | `pyproject.toml` | P0 |
| 0.5 | ORM 迁移 SQLAlchemy → Tortoise（模型/会话，开发环境用 `generate_schemas` 自动建表） | `models/`、`db/` | P0 |
| 0.6 | 更新 `.env` / `.env.example` 与 README | `.env` 等 | P1 |

### 阶段一：基础 CRUD 补全（2～3 天）

> 目标：所有设定类 API 可完整存取，数据闭环。（✅ 已完成）

| # | 任务 | 涉及文件 | 优先级 |
|---|------|----------|--------|
| 1.1 | 角色 CRUD（照 `novels.py` 模式） | `endpoints/characters.py` | P0 |
| 1.2 | 章节 CRUD + 草稿版本历史 | `endpoints/chapters.py` | P0 |
| 1.3 | 时间线事件 CRUD | `endpoints/timeline.py` | P0 |
| 1.4 | 世界观设定 CRUD（新增端点） | 新增 `endpoints/world_settings.py` | P0 |
| 1.5 | 补全 schemas（timeline / world_setting） | `schemas/` | P0 |
| 1.6 | 核对 `models/__init__.py` 已导出全部模型 | `models/__init__.py` | P1 |

### 阶段二：LLM 接入（通义千问）（2～3 天）

> 目标：通义千问可正常调用，提示词模板就绪。（✅ 已完成）

| # | 任务 | 涉及文件 | 优先级 |
|---|------|----------|--------|
| 2.1 | 实现 `LLMClient.complete()`：流式 + 聚合返回 | `llm/client.py` | P0 |
| 2.2 | 编写 planning 提示词（结构化输出：大纲/角色/地点/事件/情绪） | `llm/prompts/planning.py` | P0 |
| 2.3 | 编写 generation 提示词（事实清单作为「必须遵守的事实」注入） | `llm/prompts/generation.py` | P0 |
| 2.4 | 编写 review 提示词（输出结构化冲突列表） | `llm/prompts/review.py` | P0 |
| 2.5 | 定义工具 JSON schema（文本统计；百科检索走 DashScope 函数调用或后续接插件） | `llm/tools.py` | P1 |
| 2.6 | lifespan 中初始化 LLM 客户端 | `app/main.py` | P1 |

### 阶段三：嵌入编码与设定检索（2～3 天）

> 目标：设定写入后自动编码 embedding，可按语义召回。（✅ 已完成）

| # | 任务 | 涉及文件 | 优先级 |
|---|------|----------|--------|
| 3.1 | 实现 `EmbeddingClient`：懒加载 + `encode()`（归一化） | `embeddings/client.py` | P0 |
| 3.2 | 设定写入/更新后同步刷新 embedding（角色/世界观/时间线） | `services/settings.py` | P0 |
| 3.3 | 结构化精确查询（按 novel_id / category / 关键词） | `services/settings.py` | P0 |
| 3.4 | pgvector 余弦相似度召回（`<=>` 升序 top-k） | `services/settings.py` | P0 |
| 3.5 | 组装事实清单（渲染为紧凑文本块） | `services/settings.py` | P1 |
| 3.6 | lifespan 中懒加载嵌入模型 | `app/main.py` | P1 |

### 阶段四：生成流水线核心（4～5 天）

> 目标：单章节生成全流程可跑通。（✅ 已完成）

| # | 任务 | 涉及文件 | 优先级 |
|---|------|----------|--------|
| 4.1 | Planner：调 LLM 拆解章节目标 | `services/generation/planner.py` | P0 |
| 4.2 | Retriever：检索设定 + 组装事实清单 | `services/generation/retriever.py` | P0 |
| 4.3 | Generator：携带事实清单流式生成草稿 | `services/generation/generator.py` | P0 |
| 4.4 | Reviewer：审稿 + 输出结构化冲突列表 | `services/generation/reviewer.py` | P0 |
| 4.5 | Pipeline 串联：planner → retriever → generator → reviewer → 落库 | `services/generation/pipeline.py` | P0 |
| 4.6 | 接入 `POST /generate/chapter` | `endpoints/generate.py` | P0 |
| 4.7 | 冲突回退修正循环（`max_review_rounds=3`） | `services/generation/pipeline.py` | P1 |

### 阶段五：交互式重写与增强（3～4 天）

> 目标：支持局部重写、版本对比、批量生成。（✅ 已完成）

| # | 任务 | 涉及文件 | 优先级 |
|---|------|----------|--------|
| 5.1 | 章节局部重写（按段落范围） | `endpoints/chapters.py` | P0 |
| 5.2 | 草稿版本列表与对比 | `endpoints/chapters.py` | P1 |
| 5.3 | 批量章节生成（多章节流水线） | `services/generation/pipeline.py` | P1 |
| 5.4 | 前文摘要自动生成（供 planner 使用） | `services/` | P1 |
| 5.5 | 生成进度 WebSocket 推送 | 新增 `endpoints/ws.py` | P2 |

### 阶段六：多 Agent 编排（LangGraph）（3～5 天，可选）

> 目标：把确定性流水线升级为可自治循环、可断点续跑的 Agent 图。（✅ 已完成）

| # | 任务 | 涉及文件 | 优先级 |
|---|------|----------|--------|
| 6.1 | 用 `langchain.ChatOpenAI`（base_url 指向 DashScope）封装各 Agent | `llm/`（或新增 `agents/`） | P0 |
| 6.2 | 用 `langgraph.StateGraph` 定义节点与状态（planner/writer/reviewer/editor） | 新增 `app/agents/graph.py` | P0 |
| 6.3 | 条件路由：审稿通过 → 落库；有冲突 → 回退 writer（带轮次上限） | `app/agents/graph.py` | P0 |
| 6.4 | 接入 `PGVector` 作为 LangChain retriever（替代/并存于手写 SQL 召回） | `services/settings.py` | P1 |
| 6.5 | `checkpointer` 断点续跑（SQLite/Postgres saver） | `app/agents/graph.py` | P1 |
| 6.6 | 保留原确定性流水线作为「单章节快速生成」入口，LangGraph 作为「多 Agent 增强」入口 | `endpoints/generate.py` | P1 |

### 阶段七：质量保障与部署（2～3 天）

> 目标：测试覆盖、CI、生产就绪。（✅ 已完成）

| # | 任务 | 涉及文件 | 优先级 |
|---|------|----------|--------|
| 7.1 | 单元测试（CRUD / services / pipeline，LLM 用 mock） | `tests/` | P0 |
| 7.2 | 集成测试（真实数据库 + mock 模型响应） | `tests/` | P0 |
| 7.3 | ruff lint + 类型检查 CI | `pyproject.toml` / `.github/` | P1 |
| 7.4 | 错误处理与日志规范化 | `app/` | P1 |
| 7.5 | 生产 Dockerfile（含 embedding 权重缓存） | 新增 `Dockerfile` | P1 |
| 7.6 | 鉴权（JWT / API Key） | `app/api/deps.py` | P2 |

---

## 六、关键代码参考

### 6.1 配置（`core/config.py` 目标形态）

```python
class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", extra="ignore")

    app_name: str = "Writing Assistant API"
    environment: str = "development"
    log_level: str = "INFO"

    database_url: str = "postgres://postgres:postgres@localhost:5432/writing_assistant"

    # LLM（通义千问，OpenAI 兼容模式）
    llm_base_url: str = "https://dashscope.aliyuncs.com/compatible-mode/v1"
    dashscope_api_key: str | None = None
    generation_model: str = "qwen-max"
    review_model: str = "qwen-plus"

    # 嵌入（provider 可选 huggingface / openai）
    embedding_provider: str = "huggingface"
    embedding_model: str = "BAAI/bge-small-zh-v1.5"
    embedding_dimension: int = 512
```

### 6.2 LLM 客户端（`llm/client.py` 目标形态）

```python
from openai import AsyncOpenAI
from app.core.config import settings


def get_client() -> AsyncOpenAI:
    if not settings.dashscope_api_key:
        raise RuntimeError("DASHSCOPE_API_KEY 未配置，请在 .env 中设置")
    return AsyncOpenAI(api_key=settings.dashscope_api_key, base_url=settings.llm_base_url)


class LLMClient:
    def __init__(self, model: str | None = None) -> None:
        self.client = get_client()
        self.model = model or settings.generation_model

    async def complete(self, messages: list[dict], system: str | None = None, max_tokens: int = 8000) -> str:
        msgs = []
        if system:
            msgs.append({"role": "system", "content": system})
        msgs.extend(messages)
        resp = await self.client.chat.completions.create(
            model=self.model,
            messages=msgs,
            max_tokens=max_tokens,
            stream=False,          # 需要流式时改 stream=True 并逐段 yield
        )
        return resp.choices[0].message.content or ""
```

### 6.3 嵌入客户端（`embeddings/client.py` 目标形态）

```python
from sentence_transformers import SentenceTransformer
from app.core.config import settings


class EmbeddingClient:
    def __init__(self, model_name: str | None = None) -> None:
        self.model_name = model_name or settings.embedding_model
        self._model = None

    def _load(self) -> SentenceTransformer:
        if self._model is None:
            self._model = SentenceTransformer(self.model_name)
        return self._model

    def encode(self, texts: list[str]) -> list[list[float]]:
        return self._load().encode(texts, normalize_embeddings=True).tolist()
```

### 6.4 pgvector 余弦召回（`services/settings.py` 参考）

```python
from tortoise import connections
from app.embeddings.client import get_embedding_client
from app.models.character import Character


async def recall_characters(novel_id, query_text: str, top_k: int = 5):
    qvec = await get_embedding_client().aencode_query(query_text)
    qstr = "[" + ",".join(str(x) for x in qvec) + "]"
    conn = connections.get("default")
    rows = await conn.execute_query_dict(
        "SELECT id FROM characters "
        "WHERE novel_id = $1 AND embedding IS NOT NULL "
        "ORDER BY embedding <=> $2::vector LIMIT $3",
        [str(novel_id), qstr, top_k],
    )
    ids = [r["id"] for r in rows]
    return await Character.filter(id__in=ids).all()
```

---

## 七、开发约定

| 约定 | 说明 |
|------|------|
| 全异步 | Tortoise ORM + asyncpg，所有 IO 走 `async` |
| 主键 | UUID，`created_at` / `updated_at` 由 `DatetimeField(auto_now_add/auto_now)` 维护 |
| 生成模型 | `qwen-max`（`.env` 可覆盖） |
| 审稿模型 | `qwen-plus`（更便宜） |
| 嵌入模型 | `BAAI/bge-small-zh-v1.5`，512 维，归一化 |
| 一致性策略 | 结构化表 + embedding 检索，事实清单注入提示词 |
| 多 Agent | 需要自治循环/复杂控制流时用 LangGraph；简单场景保持确定性流水线 |
| 代码风格 | ruff，line-length=100，target=py314 |
| 测试 | pytest + pytest-asyncio，`asyncio_mode=auto` |

---

## 八、环境配置与快速启动

```bash
# 1. 起数据库（PostgreSQL + pgvector）
docker compose up -d db

# 2. 装依赖（uv 按 uv.lock 精确安装到 .venv；embeddings 可选：本地编码时才需要）
uv sync --extra dev --extra embeddings

# 3. 配置环境变量（.env）
#    DASHSCOPE_API_KEY=sk-xxx
#    DATABASE_URL=postgres://postgres:postgres@localhost:5432/writing_assistant
#    EMBEDDING_PROVIDER=huggingface
#    EMBEDDING_MODEL=BAAI/bge-small-zh-v1.5
#    EMBEDDING_DIMENSION=512

# 4. 启动（开发环境 lifespan 自动建表 + 确保 vector 扩展）
uv run uvicorn app.main:app --reload
# 文档：http://localhost:8000/docs

# 5. 测试与 lint
uv run pytest
uv run ruff check .
```

---

## 九、风险与注意事项

1. **向量维度一致性**：本地 bge-small-zh 是 512 维，远程 DashScope text-embedding-v3 默认 1024 维；切换 `EMBEDDING_PROVIDER` 时必须同步改 `EMBEDDING_DIMENSION` 并重建向量列（尚无生产数据可忽略）。
2. **模型名演进**：通义千问模型 ID 会更新，务必收敛到 `.env`，不要写死在代码。
3. **长上下文成本**：注入大量前文/设定时关注 token 成本，必要时用「前文摘要」而非全文（阶段五 5.4）。
4. **DashScope 兼容模式差异**：函数调用 / JSON 输出等参数以兼容模式文档为准，注意与原生 OpenAI 的细微差异。
5. **embedding 冷启动**：sentence-transformers 首次加载需下载权重，建议 `_load()` 懒加载 + 启动预热。
6. **LangGraph 引入时机**：先跑通确定性流水线再升级多 Agent，避免过早抽象。